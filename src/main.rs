use anyhow::Result;
// use git_info::GitInfo;

fn main() -> Result<()> {
    println!("Hello, world!");

    // let git_info = GitInfo::new(token);

    // let user = git_info.user()?;

    // println!("User: {}", user);

    Ok(())
}
